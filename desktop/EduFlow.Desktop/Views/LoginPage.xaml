<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="EduFlow.Desktop.Views.LoginPage"
             x:Name="ThisPage"
             Title="Login">

    <VerticalStackLayout Padding="24" Spacing="12">

        <Label x:Name="RoleLabel"
               Text="Login"
               FontSize="22"
               FontAttributes="Bold" />

        <!-- Teacher: Login/Register toggle -->
        <HorizontalStackLayout x:Name="TeacherModeRow"
                               Spacing="12"
                               IsVisible="False">
            <Button Text="Login" Clicked="OnTeacherLoginMode"/>
            <Button Text="Register" Clicked="OnTeacherRegisterMode"/>
        </HorizontalStackLayout>

        <!-- Student: Login/Register toggle -->
        <HorizontalStackLayout x:Name="StudentModeRow"
                               Spacing="12"
                               IsVisible="False">
            <Button Text="Login" Clicked="OnStudentLoginMode"/>
            <Button Text="Register" Clicked="OnStudentRegisterMode"/>
        </HorizontalStackLayout>

        <Entry x:Name="EmailEntry"
               Placeholder="Email"
               Keyboard="Email"/>

        <Entry x:Name="PasswordEntry"
               Placeholder="Password"
               IsPassword="True"/>

        <!-- Teacher register only -->
        <Entry x:Name="TeacherDisplayNameEntry"
               Placeholder="Display name (optional)"
               IsVisible="False"/>

        <!-- Student register only -->
        <Entry x:Name="InviteCodeEntry"
               Placeholder="Invite code"
               IsVisible="False"/>

        <Entry x:Name="GradeLevelEntry"
               Placeholder="Grade level"
               Keyboard="Numeric"
               IsVisible="False"/>

        <Button x:Name="SubmitButton"
                Text="Continue"
                Clicked="OnSubmit"/>

        <!-- Loading -->
        <ActivityIndicator x:Name="LoadingIndicator"
                           IsRunning="False"
                           IsVisible="False"
                           HorizontalOptions="Center"/>

        <Label x:Name="ErrorLabel"
               TextColor="Red"/>

    </VerticalStackLayout>
</ContentPage>